# Сценарий проверки

Гайд по ручной и автоматической проверке работоспособности CVE Monitor Bot.

## Автотесты

Без сети и без Telegram — быстрый прогон логики:

```bash
pip install -r requirements-dev.txt
pytest -q
```

Ожидается `21 passed`. Те же проверки гоняются в GitHub Actions на каждый push/PR.

## Запуск бота

```bash
pip install -r requirements.txt
cp .env.example .env     # впиши TELEGRAM_TOKEN от @BotFather
python cve_bot.py
```

Ожидается в консоли:

```
[INFO] Запускаю CVE Monitor Bot...
[INFO] Бот запущен. Нажми Ctrl+C для остановки.
```

Если включены авто-уведомления (`CHECK_INTERVAL_MIN > 0` и задан `TELEGRAM_CHAT_ID`) —
дополнительно строка `Авто-уведомления включены: каждые N мин`.

## Проверка команд в Telegram

| Шаг | Команда | Ожидаемый результат |
|---|---|---|
| 1 | `/start` | Приветствие + 4 кнопки (Сканировать, CRITICAL, Статус, Помощь). Каждая кнопка работает как команда. |
| 2 | `/help` | Список команд, включая `/cve <id>`, и легенда 🚨 KEV / 📈 EPSS. |
| 3 | `/status` | Порог CVSS, интервал авто-проверки и `NVD API-ключ: есть` (если ключ задан). |
| 4 | `/cve CVE-2021-44228` | Карточка Log4Shell: CVSS 10.0 CRITICAL, 🚨 CISA KEV, 📈 EPSS ~94%. |
| 5 | `/critical` | CVE с CVSS ≥ 9.0 за 24ч (🔴), KEV-первыми. Или «Уязвимостей не найдено». |
| 6 | `/scan` | Свежие CVE за 24ч (CVSS ≥ порога); ⚪ «оценка ожидается» для самых новых — это норма. |
| 7 | `/filter apache` | Поиск по **всей базе** NVD (не только за сутки) — должны быть результаты. |

## Проверка обработки ошибок

| Действие | Ожидаемый результат |
|---|---|
| `/filter` (без слова) | ⚠️ «Укажи ключевое слово…», бот не падает |
| `/cve` (без id) | ⚠️ «Укажи идентификатор…» |
| `/cve мусор123` | ⚠️ «Не похоже на CVE-id…» |
| `/cve CVE-1999-9999` | 🤷 «не найдена в базе NVD» |
| `/filter <b>x</b>` | спецсимволы экранируются, сообщение приходит без ошибки парсинга HTML |
| запуск без `TELEGRAM_TOKEN` | внятное `[FATAL] Не задан TELEGRAM_TOKEN`, а не стектрейс |

## Проверка авто-уведомлений

1. В `.env`: `TELEGRAM_CHAT_ID=<твой id от @userinfobot>` и `CHECK_INTERVAL_MIN=1` (для быстрого теста).
2. Перезапусти бота.
3. Через ~10 секунд придёт пачка «🆕 Новые CVE» — это нормально, `seen_cves.json` был пуст.
4. На следующем цикле в консоли — `Новых CVE нет` (дедупликация работает).
5. Рядом с `cve_bot.py` появился файл `seen_cves.json`.
6. Верни `CHECK_INTERVAL_MIN=60` (или нужное значение).

## Критерий приёмки

- Бот стартует без ошибок и отвечает на все 6 команд.
- `/status` показывает `NVD API-ключ: есть` (если ключ настроен).
- Ошибочный ввод даёт аккуратные предупреждения, а не стектрейс.
- При включённых авто-уведомлениях новые CVE приходят без повторов.

Остановка бота — `Ctrl+C`.
